# Build de produção (Vercel): same-origin via rewrite em vercel.json
# (/api/... -> backend), então o caminho relativo é suficiente — sem
# CORS, sem depender de configurar isso no dashboard da Vercel.
VITE_API_BASE_URL=/api/v1
